inherits: faces_ce.yaml
head:
  name: arcface
  s: 96.0     # s=64 undershot for this 1000-way problem -- test accuracy
              # 61.79->64.94 with the LR fix alone, then 64.63->72.71 raising
              # s here, closing the gap to CE (74.65) to 1.94pp. See
              # notes/decisions.md, 2026-09-14.
  m: 0.5
train:
  warmup_epochs: 5
